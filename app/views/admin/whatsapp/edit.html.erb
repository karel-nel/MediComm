<%= render Admin::PageHeaderComponent.new(
  title: "Edit WhatsApp Account",
  subtitle: @account.business_account_name.presence || @account.display_phone_number
) %>

<%= render "form",
  account: @account,
  form_url: admin_whatsapp_path,
  form_method: :patch,
  submit_label: "Save Account" %>
